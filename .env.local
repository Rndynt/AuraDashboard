# Database
DATABASE_URL=${DATABASE_URL}

# Auth - Use environment variables, never hardcode secrets
BETTER_AUTH_SECRET=${BETTER_AUTH_SECRET}
BETTER_AUTH_URL=${BETTER_AUTH_URL:-https://${REPLIT_DOMAINS:-localhost:5000}}
SESSION_SECRET=${SESSION_SECRET}

# Redis (for rate limiting) - Optional in development
REDIS_URL=${REDIS_URL:-}

# Observability
SENTRY_DSN=${SENTRY_DSN:-}
OTEL_EXPORTER_OTLP_ENDPOINT=${OTEL_EXPORTER_OTLP_ENDPOINT:-}

# Superuser
SUPERUSER_EMAIL=${SUPERUSER_EMAIL:-admin@company.com}
SUPERUSER_NAME=${SUPERUSER_NAME:-Super Admin}

# Security - Dynamic CORS for Replit
ALLOWED_ORIGINS=${ALLOWED_ORIGINS:-https://${REPLIT_DOMAINS:-localhost:5000}}